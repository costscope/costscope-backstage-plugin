import React from 'react';

export type BreakdownTableProps = { items?: unknown[] };

export const BreakdownTable: React.FC<BreakdownTableProps> = () => {
	return <div data-testid="breakdown-table">BreakdownTable (placeholder)</div>;
};

export default BreakdownTable;
