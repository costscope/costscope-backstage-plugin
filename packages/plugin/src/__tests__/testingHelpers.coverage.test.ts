import '../testing/contractsStorybookMock';
import '../testing/highContrastTheme';
import '../testing/simpleBackstageMocks';

test('testing helper modules load without error', () => {
  // No assertions — the import itself increases coverage for previously-unused files.
  expect(true).toBe(true);
});
