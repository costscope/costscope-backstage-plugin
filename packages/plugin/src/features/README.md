# Features directory

Domain-oriented UI + hooks grouped by API endpoint.

- Each feature keeps thin UI in `ui/` and React Query hooks in `hooks/`.
- Co-locate tests under `__tests__/`.
- Promote reusable visual blocks to `src/components/*`.
