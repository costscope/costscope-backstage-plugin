Create a changeset with `yarn changeset` when you want to record a package change for a future release.

Managed by Changesets. Create a new changeset with:

```bash
yarn changeset
```

CI workflow `.github/workflows/release-version.yml` creates/updates a version PR on pushes to `main`.
